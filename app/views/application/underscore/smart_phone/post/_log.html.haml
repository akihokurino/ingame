%script{type: "text/template", id: "log-template"}
  %a{href: "/posts/new#write/{{= game.id }}/{{= id }}"}
    %figure
      / %img.gameImg{src: "/game_photos/{{= game.photo_path }}", height: 32, alt: ""}
      %img.gameImg{src: "{{= game.photo_url }}", width: 100, alt: ""}
      %figcaption
        {{= game.title }}