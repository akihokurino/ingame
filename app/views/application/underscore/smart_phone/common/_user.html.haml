%script{type: "text/template", id: "user-template"}
  %ul.friendListWrap.btn-list
    {{ if (!isCurrentUser) { }}
    {{ if (i_followed) { }}
    %li.followBtnWrap
      .followBtn.unFollow.unfollow-btn フォローを外す
    {{ } else { }}
    %li.followBtnWrap
      {{ if (i_followered) { }}
      .followBtn.follow-btn フォロー仕返す
      {{ } else { }}
      .followBtn.follow-btn フォローする
      {{ } }}
    {{ } }}
    {{ } }}
    %figure.userImgWrap
      %img.profImg{src:"/user_photos/{{= photo_path }}" , height: 40, height: 40}
    .userInfo
      .nameWrap
        %a.name{href: "/users/{{= id }}#logs"} {{= username }}
      %span.icon-place
      .place {{= place }}
