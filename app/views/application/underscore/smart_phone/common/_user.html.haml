%script{type: "text/template", id: "user-template"}
  %ul.friendListWrap.btn-list
    {{ if (!isCurrentUser) { }}
    {{ if (i_followed) { }}
    %li.followBtnWrap
      .followBtn.unFollow.unfollow-btn フォロー中
    {{ } else { }}
    %li.followBtnWrap
      {{ if (i_followered) { }}
      .followBtn.follow-btn フォロー中
      {{ } else { }}
      .followBtn.follow-btn フォローする
      {{ } }}
    {{ } }}
    {{ } }}
    %figure.userImgWrap
      %img.profImg{src:"/user_photos/{{= photo_path }}" , height: 50, height: 50}
    .userInfo
      .nameWrap
        %a.name{href: "/users/{{= id }}#logs"} {{= username }}
      .myGame マイゲーム 125