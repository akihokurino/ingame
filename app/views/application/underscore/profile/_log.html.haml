%script{type: "text/template", id: "log-template"}
  %a.articleWrap{href: "/games/{{= game.id }}", style: "display: block;"}
    %article.game
      %img.gameImg{src: "{{= game.photo_path }}"}
      %h1.ttl
        {{= game.title}}
      %ul.stars
        {{ if (rate) { }}
        {{ for(var i = 0; i < parseInt(rate); i++) { }}
        %li.star
        {{ } }}
        {{ } }}
